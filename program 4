git clone:- copy an existing remote repository to your local machine (Creates a local copy of a remote repository)
git pull:- to update your local repository with latest change from remote repository (Synchronizes your local branch with the remote branch)
git push:- uplod your local commits to remote repository (Sends your local branch commits to the remote branch)

mkdir reponew
cd repone
git init
ls
git clone"{repository}"
ls
cd repotest
ls
echo'first push'>f1.txt
ls
git add
git commit-m"Initial commit"
git status
git remote set-url origin "https://{link}@repository"
git push origin main
{token create, settings->developer settings->personal access token->fenetrate new token,note,click first 5,then copy link}
