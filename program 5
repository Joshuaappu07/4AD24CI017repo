git clone:- copy an existing remote repository to your local machine (Creates a local copy of a remote repository)
git pull:- to update your local repository with latest change from remote repository (Synchronizes your local branch with the remote branch)
git push:- upload your local commits to remote repository (Sends your local branch commits to the remote branch)
git rebase:- moving or replaying your commits onto a new base commits (Moves or “rebases” your branch’s commits on top of another branch)

mkdir newrebaserepo19
cd newrebaserepo19
git init
git clone "{repository}"
ls
cd rebaserepo(repository name)
git branch 
git log
git checkout-b feature
git log
git branch
echo "f1 added">f1.txt
git add.
git commit-m"f1 added"
echo "f2 added">f2.tt
git add
git commit-m"f2 added"
git log
git checkout main
git log
git rebase feature
git log
git push origin main
git log
git push origin main
git remote set-url origin"https://{token@repository}"
git push origin main
git push origin feature
